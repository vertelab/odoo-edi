<?xml version="1.0"?>
<odoo>
    <record id="af_edi_as_kontor" model="ir.actions.server">
        <field name="name">EDI AF Get kontor</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="state">code</field>
        <field name="code">route = env['edi.route'].search([('id', '=', 3)])

record = env['res.partner'].browse(env['ir.model.data'].xmlid_to_res_id('__ais_import__.part_jbskr_2280'))

vals = {
  'name': 'get kontor msg',
  'edi_type': env.ref('edi_af_as.arbetssokande_kontor').id,
  'model': record._name,
  'res_id': record.id,
  'route_id': route.id,
  'route_type': 'edi_af_as_kontor',
}

message = env['edi.message'].create(vals)
message.pack()
route.run()
      </field>
    </record>
</odoo>