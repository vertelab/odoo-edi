<?xml version="1.0"?>
<odoo>
    <record id="af_edi_channel" model="ir.actions.server">
        <field name="name">EDI AF Get segment</field>
        <field name="model_id" ref="base.model_ir_actions_server"/> 
        <field name="state">code</field>
        <field name="code">
route  = env['edi.route'].search([('id', '=', env['ir.model.data'].xmlid_to_res_id('edi_af_channel.asok_segment_route'))])   
record = env['res.partner'].browse(env['ir.model.data'].xmlid_to_res_id('__ais_import__.part_jbskr_2279'))

vals = {
  'name': 'Get segment msg',
  'edi_type': env.ref('edi_af_channel.registration_channel').id,
  'model': record._name,
  'res_id': record.id,
  'route_id': route.id,
  'route_type': 'edi_af_as_segment',
}

msg = env['edi.message'].create(vals)
msg.pack()
route.run()
        </field>
    </record>
</odoo>